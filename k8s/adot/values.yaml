fullnameOverride: adot-collector

mode: deployment

image:
  repository: otel/opentelemetry-collector-contrib
  tag: "0.110.0"
  pullPolicy: IfNotPresent

serviceAccount:
  create: true

# kubernetes_sd requires cluster-wide read access to pods, services and endpoints.
clusterRole:
  create: true
  name: adot-collector
  rules:
    - apiGroups: [""]
      resources: ["nodes", "nodes/proxy", "services", "endpoints", "pods"]
      verbs: ["get", "list", "watch"]

configMap:
  create: true

config:
  receivers:
    otlp:
      protocols:
        grpc:
          endpoint: 0.0.0.0:4317

    prometheus:
      config:
        scrape_configs:
          - job_name: kubernetes-pods
            kubernetes_sd_configs:
              - role: pod
                namespaces:
                  names:
                    - kafka
                    - monitoring
                    - app
            relabel_configs:
              - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_scrape]
                action: keep
                regex: "true"
              # Istio injects prometheus.io/scrape on app pods pointing to the Envoy sidecar (port 15020/15090).
              # Drop those targets to avoid high-cardinality envoy_* metrics; gateway/worker already push OTLP.
              - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_port]
                action: drop
                regex: "15020|15090"
              - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_path]
                action: replace
                target_label: __metrics_path__
                regex: (.+)
              - source_labels: [__address__, __meta_kubernetes_pod_annotation_prometheus_io_port]
                action: replace
                regex: ([^:]+)(?::\d+)?;(\d+)
                replacement: $1:$2
                target_label: __address__
              # Expose namespace + pod name as metric labels (Strimzi Grafana dashboard
              # filters on `namespace` and `kubernetes_pod_name`).
              - source_labels: [__meta_kubernetes_namespace]
                action: replace
                target_label: namespace
              - source_labels: [__meta_kubernetes_pod_name]
                action: replace
                target_label: kubernetes_pod_name
              # Map Strimzi pod labels -> metric labels used by the official dashboard
              # (`strimzi_io_cluster`, `strimzi_io_name`, `strimzi_io_kind`).
              - source_labels: [__meta_kubernetes_pod_label_strimzi_io_cluster]
                action: replace
                target_label: strimzi_io_cluster
                regex: (.+)
              - source_labels: [__meta_kubernetes_pod_label_strimzi_io_name]
                action: replace
                target_label: strimzi_io_name
                regex: (.+)
              - source_labels: [__meta_kubernetes_pod_label_strimzi_io_kind]
                action: replace
                target_label: strimzi_io_kind
                regex: (.+)

          # cAdvisor (embedded in kubelet) exposes container_* metrics used by the
          # Strimzi dashboard CPU/Memory panels. Scraped via the API-server proxy,
          # which works on minikube without needing direct kubelet TLS access.
          - job_name: kubelet-cadvisor
            scheme: https
            tls_config:
              ca_file: /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
              insecure_skip_verify: true
            bearer_token_file: /var/run/secrets/kubernetes.io/serviceaccount/token
            kubernetes_sd_configs:
              - role: node
            relabel_configs:
              - action: labelmap
                regex: __meta_kubernetes_node_label_(.+)
              - target_label: __address__
                replacement: kubernetes.default.svc:443
              - source_labels: [__meta_kubernetes_node_name]
                regex: (.+)
                target_label: __metrics_path__
                replacement: /api/v1/nodes/$1/proxy/metrics/cadvisor

          # kubelet's own /metrics endpoint exposes kubelet_volume_stats_* (PVC disk
          # usage). Only produces data for persistent-claim storage, not ephemeral.
          - job_name: kubelet
            scheme: https
            tls_config:
              ca_file: /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
              insecure_skip_verify: true
            bearer_token_file: /var/run/secrets/kubernetes.io/serviceaccount/token
            kubernetes_sd_configs:
              - role: node
            relabel_configs:
              - action: labelmap
                regex: __meta_kubernetes_node_label_(.+)
              - target_label: __address__
                replacement: kubernetes.default.svc:443
              - source_labels: [__meta_kubernetes_node_name]
                regex: (.+)
                target_label: __metrics_path__
                replacement: /api/v1/nodes/$1/proxy/metrics

  processors:
    resource:
      attributes:
        - key: service.name
          action: upsert
          from_attribute: service.name
        - key: service.version
          action: upsert
          from_attribute: service.version
    batch:
      timeout: 5s
      send_batch_size: 1024

  exporters:
    prometheusremotewrite:
      endpoint: http://vmsingle-server.monitoring.svc:8428/api/v1/write
      resource_to_telemetry_conversion:
        enabled: true

  extensions:
    health_check: {}

  service:
    extensions: [health_check]
    pipelines:
      metrics:
        receivers: [otlp, prometheus]
        processors: [resource, batch]
        exporters: [prometheusremotewrite]

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi
